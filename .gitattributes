# Normalise line endings so contributors on different platforms produce
# identical diffs. Without this, a clone on Linux or macOS rewrites every
# line of every file on first touch.

* text=auto

*.cs      text diff=csharp
*.csproj  text eol=crlf
*.slnx    text eol=crlf
*.props   text eol=crlf

*.sh   text eol=lf
*.yml  text eol=lf
*.yaml text eol=lf
